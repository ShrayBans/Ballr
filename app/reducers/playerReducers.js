import TEST from '../actions/application'
const initial_state = {
	players: []
};

export default function reducer(state= initial_state, action) {
	switch(action.type) {
		case:
			return {...state, }
		case: 
			return {...state, }
		default:
			return state;
	}
}